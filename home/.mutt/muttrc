# Global settings
set realname = 'Shane Iler'

# Barracuda defaults
set folder = imaps://mail.barracuda.com
set record = "=Sent Items"
set postponed = "=Drafts"

# IMAP settings
unset imap_passive
set imap_user = siler
set imap_keepalive = 300
set imap_check_subscribed
set mail_check = 60

# SMTP settings
set smtp_url = smtps://siler@mail.barracuda.com

set spoolfile = +INBOX

# Caching
set header_cache = "~/.mutt/cache"
set message_cachedir = "~/.mutt/cache"
